{
  "name": "nailsbyyadi-site",
  "version": "1.0.0",
  "description": "Nails by Yadi — Booking system with virtual queue, automated reminders, and owner dashboard",
  "private": true,
  "scripts": {
    "start": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "resend": "^4.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
